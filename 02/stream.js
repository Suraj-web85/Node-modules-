const fs = require('fs')
const os = require('os')
const path = require('path')

// const rs = fs.createReadStream('./files/lorem.txt', {encoding: 'utf8'})
// const ws = fs.createWriteStream('./files/new-lorem.txt')
  // rs.on('data', (dataChunk) => {
  //     ws.write(dataChunk)
  // })
  // fs.readFile(path.join(__dirname,'files','lorem.txt'),'utf8' , (data,err) =>{
  //   if(err) throw err
  //   console.log(data)
  // })
  // console.log('suraj')

  


  // rs.pipe(ws)